#include "colors.hpp"

namespace colors {
    const TCODColor lightWall(45, 35, 30);
    const TCODColor darkWall(30, 30, 30);
    const TCODColor darkerWall(20, 20, 20);
    const TCODColor lightGround(90, 80, 75);
    const TCODColor darkGround(40, 40, 40);
    const TCODColor darkerGround(20, 20, 20);
    const TCODColor bgDefault(15, 20, 25);
    const TCODColor player(30, 75, 85);
    const TCODColor zombie(0, 85, 0);
    const TCODColor darkWizard(125, 30, 90);
    const TCODColor skeletonArcher(220, 220, 220);
    const TCODColor imp(215, 75, 15);
    const TCODColor berserker(165, 0, 5);
    const TCODColor poltergeist(150, 195, 210);
    const TCODColor lich(215, 0, 10);
    const TCODColor grimReaper(55, 0, 50);
}
